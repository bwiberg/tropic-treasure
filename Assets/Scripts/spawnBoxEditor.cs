using UnityEngine;
#if UNITY_EDITOR
using UnityEditor;
#endif

/*#if UNITY_EDITOR
[CustomEditor(typeof(spawnBox))]
public class spawnBoxEditor : Editor {

    public override void OnInspectorGUI()
    {
        DrawDefaultInspector();

        var generator = (spawnBox)target;
        if (GUILayout.Button("Spawn Enemy"))
        {
            generator.spawnEnemy();
        }
    }
}
#endif*/